# dts-tasks-frontend

Simple React-based interface for creating HMCTS caseworker tasks.  
Sends POST requests to the backend API and displays the created task response.

## Usage
npm install
npm run dev

Backend required at http://localhost:5000/api
